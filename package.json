{
  "name": "app-atlas",
  "private": true,
  "type": "module",
  "scripts": {
    "build:facts": "node scripts/build-country-facts.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "country-json": "^2.3.0",
    "world-countries": "^5.1.0"
  }
}
